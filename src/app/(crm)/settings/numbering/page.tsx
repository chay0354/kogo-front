'use client';

import NumberingSection from './NumberingSection';

/**
 * מספור מסמכים — kogo's number runs, and continuing the previous software's
 * numbering in them so the books stay one consecutive run per document type.
 */
export default function SettingsNumberingPage() {
  return <NumberingSection />;
}
