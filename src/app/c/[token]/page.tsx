// The short path parents receive: `/c/<token>`. Same page as /card-link/<token>,
// which stays so links sent before the short path existed keep opening.
export { default } from '../../card-link/[token]/page';
